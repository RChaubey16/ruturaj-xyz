import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["miro.medium.com", "icon.icepanel.io", "assets.streamlinehq.com", "tanstack.com"]
  }
};

export default nextConfig;
